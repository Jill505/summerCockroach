using UnityEngine;

public class testCameraTracker : MonoBehaviour
{
    public float cameraOffsetX = 0;
    public float cameraOffsetY = 1.67f;
    public float cameraOffsetZ = -4.63f;    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
